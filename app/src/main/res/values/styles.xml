<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColor">@android:color/white</item>
        <item name="editTextStyle">@style/BaseEditText</item>
        <item name="android:fontFamily">@font/font</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:buttonStyle">@style/BaseButton</item>
    </style>

    <style name="BaseRatingBar" parent="@style/Widget.AppCompat.RatingBar.Small">
        <item name="android:numStars">5</item>
        <item name="android:stepSize">.5</item>
        <item name="android:progressDrawable">@drawable/bg_rating_bar</item>
    </style>

    <style name="BaseTextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/text_s</item>
    </style>

    <style name="BaseButton" parent="@style/Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_btn_primary_rippled</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="BaseEditText" parent="@style/Widget.AppCompat.EditText">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">#80c2c2c2</item>
        <item name="android:colorAccent">@android:color/white</item>
        <item name="android:backgroundTint">@android:color/white</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@android:color/white</item>
    </style>
</resources>
